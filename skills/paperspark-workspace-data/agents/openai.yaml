interface:
  display_name: "PaperSpark Data"
  short_description: "Query local PaperSpark workspace data via service or snapshot"
  default_prompt: "Use $paperspark-workspace-data to inspect my PaperSpark knowledge base, immersive full text, assets, or conversations."
